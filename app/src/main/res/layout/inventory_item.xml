<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <import type="com.whfrp3.tools.BindingUtils" />

        <import type="com.whfrp3.model.player.inventory.Equipment" />

        <variable
            name="item"
            type="com.whfrp3.model.player.inventory.Item" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:orientation="horizontal"
        android:paddingBottom="@dimen/small_padding">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:padding="@dimen/small_padding"
            android:src="@drawable/ic_check_black"
            android:visibility="@{(item.isEquipable &amp;&amp; ((Equipment)item).isEquipped) ? View.VISIBLE : View.GONE}" />

        <!--Content-->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="@dimen/small_padding">

            <!--Name-->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/small_padding"
                android:hint="@string/dummy_string"
                android:text="@{item.name}"
                android:textSize="@dimen/medium_text_size" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:baselineAligned="false"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/quantity_label" />

                <TextView
                    style="@style/InventoryItemQualityOrEncumbrance"
                    android:text="@{item.quantity}" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/encumbrance_label" />

                <TextView
                    style="@style/InventoryItemQualityOrEncumbrance"
                    android:text="@{item.encumbrance}" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</layout>